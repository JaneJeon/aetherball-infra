{
  "scripts": {
    "lint": "run-s lint:*",
    "lint:js": "prettier --check .",
    "format": "run-s format:*",
    "format:js": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.0",
    "npm-run-all2": "^7.0.2",
    "prettier": "^3.5.3"
  }
}
