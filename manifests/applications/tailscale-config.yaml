apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tailscale-config
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/JaneJeon/aetherball-infra
    targetRevision: HEAD
    path: manifests/tailscale
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
