apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hubble-ui
  namespace: hubble
spec:
  project: default
  source:
    repoURL: https://github.com/JaneJeon/aetherball-infra
    targetRevision: HEAD
    path: manifests/hubble
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
